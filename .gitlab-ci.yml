variables:
  BUNDLE_VERSION: master
  INSTALL_DIR: /root/src
  RUN_ITZIK: https://git.infinidat.com/infradev/helpers/raw/master/run_itzik.py
  RUN_ITZIK_PARAMETERS: --no-dev --src $INSTALL_DIR --bundle new-infra --bundle-version $BUNDLE_VERSION --until $CI_PROJECT_NAME -p $CI_PROJECT_DIR[testing] --curr-branch $CI_COMMIT_REF_NAME
  RUN_ITZIK_PARAMETERS_27: --no-dev --src $INSTALL_DIR --bundle new-infra-27 --bundle-version $BUNDLE_VERSION --until $CI_PROJECT_NAME -p $CI_PROJECT_DIR[testing] --curr-branch $CI_COMMIT_REF_NAME

stages:
  - test
  - deploy
  - post

.test:
  tags:
    - infradev
  except:
    - /^customer-.*$/
  script:
    - curl -sL $RUN_ITZIK | python - $RUN_ITZIK_PARAMETERS_27
    - source $INSTALL_DIR/env/bin/activate
    - pylint --rcfile .pylintrc -j $(nproc) infinisdk scripts tests
    - pytest -ra -W error::DeprecationWarning

py27:
  extends: .test
  image: python:2.7

py35:
  extends: .test
  image: python:3.5

py36:
  extends: .test
  image: python:3.6

py37:
  extends: .test
  image: python:3.7

docs:
  extends: .test
  image: python:3.7
  script:
    - pip install -e ".[doc]"
    - sphinx-build -a -W -E doc build/sphinx/html

pypi:
  image: python
  tags:
    - infradev
  stage: deploy
  only:
    - master
    - tags
    - /^(dev|stable)-\d+(\.\d+)*$/
  script:
    - curl -sL http://goto/pypi_register | python - -v ./

trigger:
  image: python
  tags:
    - infradev
  stage: post
  only:
    - master
    - /^(dev|stable)-\d+(\.\d+)*$/
  script:
    - curl --request POST --form "token=$CI_JOB_TOKEN" --form "ref=$CI_COMMIT_REF_NAME" https://git.infinidat.com/api/v4/projects/222/trigger/pipeline
